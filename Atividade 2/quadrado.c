#include <stdio.h>
#include <math.h>

int main(int argc, char* argv[]){

double area, lado;
printf("Digite o comprimento do lado: ");
scanf("%lf", &lado);
area = pow(lado, 2);
printf("A area do quadrado eh: %.1lf\n", area);


    
}